openapi: '3.0.0'
info:
  title: Mixed Combinator Test API
  version: '1.0'
paths:
  /test:
    get:
      operationId: test
      responses:
        '200':
          description: OK
components:
  schemas:
    MixedCombinator:
      oneOf:
        - $ref: '#/components/schemas/TypeA'
      anyOf:
        - $ref: '#/components/schemas/TypeB'
    TypeA:
      type: object
      properties:
        a:
          type: string
    TypeB:
      type: object
      properties:
        b:
          type: string
