{
  "openapi": "3.0.2",
  "info": { "title": "defaults", "version": "1.0" },
  "paths": {},
  "components": {
    "schemas": {
      "Defaults": {
        "type": "object",
        "properties": {
          "mode": {
            "type": "string",
            "enum": ["A", "B", "C"],
            "default": "B"
          },
          "retries": {
            "type": "integer",
            "format": "int32",
            "default": 3
          },
          "tags": {
            "type": "array",
            "items": { "type": "string", "enum": ["X", "Y"] },
            "default": ["X", "Y"]
          },
          "note": {
            "type": "string"
          }
        }
      }
    }
  }
}
